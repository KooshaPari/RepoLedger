{
  "name": "repo-ledger",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean",
    "bun:dev": "bun --bun pnpm -r run dev",
    "bun:build": "bun --bun pnpm -r run build",
    "bun:lint": "bun --bun pnpm -r run lint",
    "bun:typecheck": "bun --bun pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  }
}
